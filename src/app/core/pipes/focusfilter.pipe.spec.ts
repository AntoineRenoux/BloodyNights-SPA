import { FocusfilterPipe } from './focusfilter.pipe';

describe('FocusfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FocusfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
